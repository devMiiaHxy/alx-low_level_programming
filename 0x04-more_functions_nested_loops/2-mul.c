#include "main.h"
#include <stdio.h>

/**
  * mul - multiples two numbers
  * @x: The first number
  * @y: The second number
  * Return: Always 0
  */
int mul(int x, int y)
{
int result = x * y;
return (result);
}
