#include <stdio.h>
/**
  *main - Entry point of every program
  *Description: Possible combination of 2 digit numbers
  *Return: Always 0
  */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
